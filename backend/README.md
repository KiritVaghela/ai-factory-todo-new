# Backend

This is the backend for the ToDo application.

## Linting

To ensure code quality and catch syntax or style errors, run the linter using:

```bash
flake8 .
```

Make sure you have flake8 installed:

```bash
pip install flake8
```

## Running the backend

To run the backend server:

```bash
uvicorn main:app --reload
```
